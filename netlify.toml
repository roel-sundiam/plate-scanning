[build]
  command = "cd frontend && npm install && npm run build"
  publish = "frontend/dist/browser"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
